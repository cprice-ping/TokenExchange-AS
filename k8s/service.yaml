apiVersion: v1
kind: Service
metadata:
  name: token-as
  namespace: REPLACE_WITH_EXISTING_NAMESPACE
spec:
  type: ClusterIP
  selector: {app: token-as}
  ports:
    - {name: http, port: 80, targetPort: http}
